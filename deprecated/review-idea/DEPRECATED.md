# DEPRECATED

Superseded by [`/plan-with-docs`](../../plan-with-docs/) on 2026-05-11.

`review-idea`'s 5-phase pipeline (interview → tribunal → jury → verdict) is preserved inside `plan-with-docs` as the tribunal stress-test, gated behind an opt-in prompt at the end of Phase 2.

Kept here for historical reference. NOT symlinked into `~/.claude/skills/` and NOT loaded by Claude Code.
